"use client";

/**
 * CreateSpaceForm stub - space creation is not needed with CF Access.
 * Users are automatically provisioned when they first authenticate.
 */
export function CreateSpaceForm() {
  return null;
}
